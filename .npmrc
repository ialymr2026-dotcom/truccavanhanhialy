# .npmrc
# Ensure optional dependencies are installed for the target platform
# This can help with Vite/Rollup native modules
# But on Vercel, it's usually handled by their build system
# Adding this as a precaution based on the error message
optional=true
